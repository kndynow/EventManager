@using Models.SignUp;

<EditForm Model="@SignUpModel" OnValidSubmit="HandleSignUp">
	<div>
		<DataAnnotationsValidator />

		<h1>Authenticate</h1>
		<div>
			<label for="Username">Enter username: </label>
			<InputText id="Username" @bind-Value="@SignUpModel.Username" />
			<ValidationMessage For="@(() => SignUpModel.Username)"/>
		</div>

		<div>
			<label for="Password">Enter password: </label>
			<InputText id="Password" type=password @bind-Value="@SignUpModel.Password"/>
			<ValidationMessage For="@(() => SignUpModel.Password)" />

		</div>
		<div>
			<label for="RepeatPassword">Repeat your password</label>
			<InputText id="RepeatPassword" type=password @bind-Value="@SignUpModel.RepeatPassword"/>
			<ValidationMessage For="@(() => SignUpModel.RepeatPassword)" />
		</div>

		<button type="submit">Log in</button>
	</div>
</EditForm>


@code {
	[SupplyParameterFromForm]
	private SignUpModel SignUpModel { get; set; } = new SignUpModel();

	private void HandleSignUp()
	{
		Console.WriteLine($"Username: {SignUpModel.Username}");
	}
}
